function getTriArea(base : number, height : number): void {
    let area : number = 0; // 넓이

    // 삼각형의 넓이를 계산한다.
    area = base * height / 2;
    // 계산한 넓이를 표시한다.
    console.log(`넓이는 ${area}입니다.`);
}

// [리스트 7-4]의 작성 위치