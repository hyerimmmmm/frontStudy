enum Weekday {
    Sunday,
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday
}
	
const wd: Weekday = Weekday.Thursday;   // 열거 타입 변수를 선언하고 초기화한다.
console.log(wd);    // 4를 출력한다.
    