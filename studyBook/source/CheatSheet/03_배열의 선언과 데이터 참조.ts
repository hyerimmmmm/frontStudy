// 타입 주석을 이용한 배열 선언
const numbers: number[] = [1, 2, 3, 4, 5];

// 타입 추론에 의한 선언
const colors = ["red", "green", "blue"];

// 값 참조
const firstColor: string = colors[0]; // "red"
const secondColor: string = colors[1]; // "green"